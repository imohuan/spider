## 2026-07-04

### build.py 新增 --web 模式
- 新增 `build_web()` 函数和 `--web` 命令行参数
- 行为：构建前端 Vite → web/static/ + 可选 venv 安装，跳过 Playwright 浏览器和 PyInstaller
- 适用场景：Linux 无头终端部署，直接 `python main.py --serve` 访问 http://<IP>:5000
- Flask 本身已有 `create_app(static_folder='web/static')` 支持，无需额外改动
