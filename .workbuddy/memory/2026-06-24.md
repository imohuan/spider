# 2026-06-24

## 爬虫启动模式分离

- `dev.py`：注释掉 `scheduler_thread.start()`，开发模式下 Web 后台启动后爬虫不自动跑
- `main.py`：`--serve` 分支改为 `server_thread.join()` 守护等待，不调用 `scheduler.run()`
- 纯爬虫模式（无 `--serve`）行为不变，仍自动跑主循环

## 修复 /api/crawler/start 无效问题

- 根因：`scheduler.start()` 只改标志位，不启动 `scheduler.run()` 主循环线程
- 修复：`crawler_control.py` 新增 `_scheduler_thread` 全局变量，`/start` 端点检测线程存活状态，按需 `Thread.start()`
- `dev.py` 删除冗余的 `_run_scheduler` 函数和 `scheduler_thread` 变量、`import threading`

## 重构 start/stop 生命周期（2026-06-24）

- `crawler_control.py` 全量重写，核心逻辑：
  - `/start`：线程存活则只清 paused 标志；线程已死且 shutdown_event 置位则调 `_rebuild_crawler()` 重建 browser/RequestPool/Scheduler
  - `/stop`：立即返回 `stopping`，后台线程 `_do_stop()` 完成 scheduler.stop → 等线程退出 → browser.close_sync → 关 event loop
  - `/status`：融合线程存活 + scheduler Event，返回真实状态（running/paused/stopping/stopped）
  - `_rebuild_crawler()`：新建 event loop + browser + RequestPool + Scheduler，更新 `_components` 字典
- `dev.py`：调用 `init_components()` 注入完整组件字典 + `_headless/_channel/state_machine`
- `main.py`：同步更新，改为 `init_components()` 替换原来的 `init_browser()`

## 58-ai-check workflow 集成高德地图附近搜索

- `workflows/58-ai-check.py`：新增步骤 9（高德周边搜索）
  - `_geocode_address()`：调高德 /v3/geocode/geo，地址 → 经纬度
  - `_search_nearby()`：调 /v3/place/around，关键词"餐饮|餐厅|厨具|商场|超市"，半径 1km，最多 20 条
  - `execute()` 末尾从 ConfigManager 读 `amap_webapi_key`，将结果写入返回 dict 的 `nearby_pois`
  - 依赖 httpx（已在 requirements.txt）
- `web/api/shengyi.py`：新增 `_parse_nearby_pois()`，list/detail 两个接口都将 `nearby_pois` 透传给前端
- `web-ui/src/views/ShengyiAIView.vue`：详情面板 AI 评估区块下方新增「周边商家」section，展示 POI 列表（序号、名称、类型、地址、距离、评分、人均、电话、首图）

## DataBrowserView 行详情弹窗（2026-06-24）

- 点击任意表格行打开 AxDialog，宽 `max-w-[80vw]`，内容区高 `calc(80vh - 7rem)` 撑满 80%
- 字段展示规则：JSON 字符串 → AxJsonViewer（expand-level=1）；长文本(>60char) → pre；null/空 → italic null；普通 → font-mono
- 字段名列固定 144px（`w-36 shrink-0`），值列自适应 + `min-w-0 overflow-hidden`
- workflow_queue 的「重新入队」图标按钮 `@click.stop` 防止触发行点击

## DataBrowserView workflow_queue 重新入队功能（2026-06-24）

- **后端**：`web/api/workflows.py` 新增 `POST /api/workflows/tasks/<id>/requeue`
  - 将指定任务 status → `pending`，清空 result/error/started_at/finished_at
  - 任务不存在返回 404
- **前端 API**：`web-ui/src/api/index.ts` 新增 `workflowsApi`（list/run/tasks/getTask/requeue）
- **前端 UI**：`DataBrowserView.vue` 当 `selectedTable === 'workflow_queue'` 时额外渲染「操作」列
  - 按钮：「重新入队」（AxButton outline + refresh icon）
  - 状态 pending/running 时禁用按钮，避免重复触发
  - 点击后 loading 态 → 调 requeue API → 刷新当前页数据
- 前端已重新构建并同步到 `web/static/`

## ShengyiAIView 标签系统 + 排序（2026-06-24）

### 后端
- `core/storage.py`：新增 `shengyi_tags` 表（info_id + tag, UNIQUE, 含索引 idx_shengyi_tags_info / idx_shengyi_tags_tag）
- `web/api/shengyi.py`：
  - `GET /api/shengyi/tags`：列出所有去重标签
  - `GET/POST/DELETE /api/shengyi/<info_id>/tags`：获取/添加/删除 item 标签
  - list 接口新增 `tag` 筛选（逗号分隔 OR 逻辑）、`sort_by`（id/score）、`sort_order`（asc/desc）
  - list/detail 响应均包含 `custom_tags` 字段（批量加载，一次查询）
  - `_build_where` 新增 tag 子查询 IN 筛选

### 前端
- `web-ui/src/api/index.ts`：`shengyiApi` 新增 allTags / itemTags / addTag / removeTag
- `web-ui/src/views/ShengyiAIView.vue`：
  - 顶部栏：排序下拉（最新入库 / AI 评分）+ 升降序切换按钮；标签筛选 chip 行（选中即搜索）
  - 卡片：自定义标签显示（primary 色，hover 显示 × 可点击移除）；+ 按钮展开内联输入框（回车/勾确认，× 取消）；所有操作 e.stopPropagation 防止触发卡片选中
- 修复 `DataBrowserView.vue` 多余的 `</div>` 导致构建失败
- 自定义标签行左右布局（标签左 + 添加按钮右，`justify-between`，标签行可换行）

### 测试
- `tests/test_storage.py`：表计数 10→11，索引 expected 补充 shengyi_tags 的两个索引
- 459 测试全过，web-ui 构建零错误

## Chrome 扩展：按钮改为 Browser + HTML 直解析（2026-06-24）

- `chrome/content.js`：HTTP 按钮替换为「HTML 解析」
  - 按钮 1「Browser 抓取」：POST /api/queue {url, fetch_mode: 'browser'}
  - 按钮 2「HTML 解析」：POST /api/queue {url, html: document.documentElement.outerHTML} → 自动 fetch_mode=raw
  - HTML 模式取 `document.documentElement.outerHTML`，跳过 Playwright/HTTP 抓取
  - 文案图标同步更新（文档图标换掉终端图标），toast 用 `data.queue_id`


## Raw 模式：跳过抓取直接解析 HTML（2026-06-24）

- 需求：前端传入 HTML 文本，后端跳过 browser/http 抓取，直接调用 parser.parse()
- `web/api/queue.py` `create_task`：新增 `html` 参数，传入后自动 `fetch_mode="raw"`，HTML 存入 `request_config.html`
- `core/request_pool.py`：新增 `_process_raw()` 方法
  - 从 request_config 提取 HTML → 创建最小 request 记录 → 调 parser.parse() → 走 `_finish_request` 共享后处理
  - HTML 为空时直接 mark_failed
- `tests/test_request_pool.py`：4 个单元测试（正常解析/空HTML/解析异常/原始响应保存）
- `tests/test_cookie_presets_api.py`：2 个集成测试（raw 模式入队/带 parser_name 入队）
- 464 测试全过，零回归

