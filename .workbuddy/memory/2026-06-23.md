# 2026-06-23 工作日志

## 12:03 Cookie 预设功能完整实现

**设计决策（brainstorming）：**
- 存储：独立表 `cookie_presets`（id/name/domain/cookies_json/enabled/created_at/updated_at）
- 匹配策略：域名精确匹配（`urlparse(url).netloc`）
- 注入时机：入队时匹配写入 `queue.request_config.cookies`（不覆盖前端已有）
- 前端：AxDialog 弹窗 + EditThisCookie JSON 粘贴输入

**实现（Subagent-Driven，7 任务）：**

| # | 文件 | 内容 |
|---|------|------|
| 1 | `core/storage.py` | `cookie_presets` 表 + 6 CRUD 方法（upsert/get/list/delete/match/toggle） |
| 2 | `web/api/cookie_presets.py` | API 蓝图（GET/POST/DELETE/toggle/get） |
| 3 | `web/api/queue.py` | 入队自动匹配 + `_convert_edit_this_cookie` 转换 |
| 4 | `core/request_pool.py` | `_extract_cookies_from_task` + `_browser_page_lifecycle(cookies=)` + Browser/CDP 分支注入 |
| 5 | `web-ui/` | CookiePresetsView.vue + 路由 + API + 侧栏导航 |
| 6 | `tests/test_cookie_presets_api.py` | 4 个集成测试（CRUD/入队匹配/不覆盖） |
| 7 | 全量回归 | 442 tests PASS，零回归 |

**三种模式 Cookie 支持状态：**
- HTTP：`_fetch_http()` 已有 cookies 支持（Task 3 入队匹配生效）
- Browser：新增 `page.context.add_cookies()` 在 `goto` 前（Task 4）
- CDP：同 Browser，复用 `_browser_page_lifecycle`

**测试：** 442/442 pass (原 394 + 新增 48)

## 09:06 前端UI修复："手动拉取 10 个 IP" → "手动获取 1 个 IP"
- 问题：ProxyPoolView.vue 按钮文案仍显示"手动拉取 10 个 IP"，与 JIT 模型（逐个获取）不一致
- 修复：
  - 按钮文案：`手动拉取 10 个 IP` → `手动获取 1 个 IP`
  - 通知文案：`正在拉取 IP...` → `正在获取 1 个 IP...`
  - 后端 `web/api/proxy.py:fetch_proxy` 从空壳 stub 改为实际调用 `provider.fetch(num=1)` 并 INSERT 入库
- 全量 453 通过

## 09:13 前端：所有 table 横向滚动 + 全局滚动条样式
- 目标：所有表格添加横向滚动，全局滚动条无背景、仅滑块、无圆角
- 全局样式（style.css）：`::-webkit-scrollbar` 系列 + Firefox `scrollbar-width: thin`，track 透明，thumb `rgba(0,0,0,0.2)`，`border-radius: 0`
- 4 个表格视图：QueueView / DataBrowserView / ProxyPoolView / CaptchaLogView，外层保持 `overflow-hidden`（裁剪圆角），table 外包 `<div class="overflow-x-auto">` 实现横向滚动
- DataBrowserView 特殊处理：`v-if` 移到 `overflow-x-auto` div 上，保持 `v-else` 空状态兄弟关系
- CrawlerSettingsDialog 已有 `overflow-x-auto`，无需改动
- 构建通过（仅 rolldown 预存 annotation 警告）

## 09:20 新增预览UI Tab
- 目标：在"数据"分组上方添加"预览UI"tab，AI 生成 HTML 模板 + {{字段}} 占位符 + 数据渲染预览
- 新增文件：`src/views/PreviewUIView.vue`
  - 左侧可折叠 AI 配置面板（AxInput multiline prompt + AxSelect 数据表选择 + AxButton 生成）
  - 右侧预览区：折叠按钮 + 6 个 AxButton 列数选择(1-6) + CSS Grid 预览 + AxPagination 分页
  - 模板引擎 `{{xxx}}` → 行数据替换，v-html 渲染
  - 生成按钮自动根据选定表字段名（img/title/price/desc/loc/date）生成卡片式 HTML
- 修改文件：
  - `src/router/index.ts`：添加 `/preview` 路由（在 /data 前）
  - `src/components/layout/CrawlerSidebar.vue`：数据分组添加"预览UI"导航项（icon: grid_view）
- vue-tsc + vite build 通过，零 type error

## 09:29 预览UI布局调整
- 折叠按钮 + 列数选择器(1-6) 移至底部与分页同行，flex 布局：`[折叠] [列数 1-6] [flex-1 占位] [分页]`
- 左侧AI配置面板内部移除 `p-4`，改用 `px-4` 逐项控制间距
- 所有组件 size 统一 `lg`（AxButton、AxSelect、AxInput）

## 09:36 预览UI接入AI生成
- 后端：`web/api/config_api.py` 新增 `POST /api/config/generate-template`
  - 接收 `table` + `prompt`，查首尾行数据，构建 system prompt + 字段/示例数据 → AI chat completion
  - 返回纯 HTML 模板（含 `{{字段}}` 占位符），自动清理 markdown 代码块标记
  - 使用 config 中的 ai_base_url/api_key/model 配置
- 前端：
  - `src/api/index.ts` 新增 `configApi.generateTemplate({ table, prompt })`
  - `PreviewUIView.vue` 的 `generateTemplate` 改为真实 API 调用，添加 `generating` loading 状态
- 全量 454 测试通过，vue-tsc 零错误

## 09:40 修复 Ctrl+C 关闭 dev.py 的 RuntimeError
- 问题：`dev.py` 按 Ctrl+C → `_shutdown` → `stop_image_worker` → `cancel_all_tasks` → `loop.run_until_complete()` 报 `RuntimeError: This event loop is already running`
- 根因：`_img_loop` 在 worker 线程里跑着 `run_until_complete(img_worker.run())`，主线程又调 `loop.run_until_complete()` 试图 cancel tasks — 同一个 loop 不能被两个线程同时 `run_until_complete`
- 修复 `core/bootstrap.py` 两处：
  1. `cancel_all_tasks`：增加 `loop.is_running()` 检测，running 时走 `asyncio.run_coroutine_threadsafe(_drain(), loop)` 在 loop 所在线程内执行 cancel + gather
  2. `stop_image_worker`：调整顺序 — `worker.stop()` → `loop.call_soon_threadsafe(loop.stop)` → `thread.join()` → `cancel_all_tasks()`（loop 已停止，走 `run_until_complete` 分支安全）
  3. `start_image_worker._run_worker`：catch `RuntimeError` + `CancelledError`（正常关闭路径，无需报错）

## 09:47 模板缓存系统
- 后端：
  - `core/storage.py` INIT_SQL 新增 `templates` 表（id/table_name/template_html/template_name/created_at/updated_at + 索引）
  - `web/api/config_api.py` 新增 3 个 API 端点：
    - `GET /api/config/templates?table=xxx` — 列出该表所有模板
    - `POST /api/config/templates` — 新建/更新（传 id 则更新）
    - `DELETE /api/config/templates/<id>` — 删除
  - `generate-template` 成功后自动 INSERT 到 templates 表
  - `web/api/data.py` 数据浏览 exclude 列表加入 templates/image_queue
- 前端：
  - `api/index.ts` 新增 `getTemplates/saveTemplate/deleteTemplate`
  - `PreviewUIView.vue` 重构：
    - HTML 模板区域改为可编辑 `AxInput multiline`，检测 `htmlDirty` 后显示"保存为新模板"按钮
    - 左侧底部显示模板卡片列表 — 用首行数据渲染预览，点击选中
    - 每张卡片带删除按钮；切换数据表自动加载对应模板
    - 生成成功后自动刷新模板列表
- 测试：`test_storage.py` 期望表数 7→8，全量 454 通过

## 10:51 高德地图附近搜索组件封装
- 目标：将 amap-nearby-search.html 封装为 Vue 组件，API Key/安全密钥放配置中
- 后端：
  - `core/config_manager.py`：`_DEFAULT_CONFIGS` 新增 `amap_key` 和 `amap_security_code` 两项
- 前端新增/修改 5 个文件：
  - `src/views/ConfigView.vue`：新增"地图"Tab，含 `amap_key`/`amap_security_code` 文本字段（第 7 个 tab，index=6）
  - `src/views/AmapNearbySearchView.vue`：核心组件 — ax-ui-kit（AxInput/AxSelect/AxButton size=lg）+ AMap JSAPI 2.0 动态加载 + 附近搜索 + 截图 + 定位 + 详情面板
  - `src/router/index.ts`：新增 `/amap` 懒加载路由
  - `src/components/layout/CrawlerSidebar.vue`：数据分组添加"地图搜索"导航（icon: map）
- 组件特性：从 config API 读取 amap_key/security_code；搜索支持 radius/sort AxSelect；结果列表 + 标记渲染；详情侧滑面板；html2canvas 截图；浏览器定位
- 全量 455 测试通过，vite build 成功（仅 rolldown 预存 warning）

## 11:07 AmapNearbySearch 组件化重构
- 目标：封装为可复用组件，props 传入参数，v-model 绑定，暴露 search()/openDetail()
- 新增 `src/components/AmapNearbySearch.vue`（~450行）：
  - Props: `amapKey`(required), `amapSecurityCode`(optional)
  - v-model: `lng`, `lat`, `radius`, `sortBy`, `keyword`（全部 defineModel）
  - defineExpose: `search()`, `openDetail(index)`
  - 移除 configApi（由父组件负责加载配置）
- 重写 `src/views/AmapNearbySearchView.vue`（~80行 thin wrapper）：
  - onMounted 加载 configApi → 传 props 给子组件
  - useRoute/useRouter 实现 query 参数实时同步到 URL（300ms debounce）
  - 未配置状态提示 + 跳转配置页
- vite build 成功，零新增 error

## 11:27 新增 AmapApiSearch 纯 REST API 搜索
- 目标：不加载地图 JS SDK，仅用 fetch() 调高德 REST API 搜索
- 新增 `src/components/AmapApiSearch.vue`（~160行）
  - Props: `amapKey`；v-model: lng/lat/radius/sortBy/keyword
  - `GET /v3/place/around` → 结果卡片（图标/名称/距离/评分/照片/电话/地址）
  - 点击卡片跳转高德地图详情页（`https://ditu.amap.com/place/{id}`）
  - defineExpose: `search()`
- 新增 `src/views/AmapApiSearchView.vue`（~60行）— URL sync wrapper
- 路由: `/amap-api`，侧边栏: `travel_explore` 图标
- API 搜索不需要 securityJsCode，只需 Web服务 Key

## 11:33 删除独立 API 搜索页面，改为按钮集成
- 删除 `AmapApiSearch.vue` / `AmapApiSearchView.vue`，回退路由和侧边栏
- 在地图搜索侧边栏的「搜索」按钮右侧新增「API」按钮
- `doApiSearch()` 调 REST API，结果统一走 `addMarkers` 渲染到地图
- 未配置 `amap_webapi_key` 时按钮 disabled
- `amapWebapiKey` 作为 optional prop 传入组件

## 11:50 修复切 tab 后地图不显示
- 根因：`<transition>` 销毁重建组件，`initMap()` 时 DOM 容器尚未完成布局，Map 拿到 0 尺寸
- 修复：`initMap()` 前后各加 `await nextTick()`，初始化后调 `map.resize()`

## 12:21 新增天眼查搜索页 Parser 插件
- 位置：`parser/plugins/tianyancha/search.py`
- 类名：`TianyanchaSearchParser`，继承 `BaseParser`
- URL 匹配：`tianyancha\.com/nsearch`
- 模式：HTTP 优先（`preferred_fetch_mode = "http"`），SSR 页面无需浏览器
- 核心流程：
  1. 字符串定位提取 `<script id="__NEXT_DATA__">` JSON（避免大 JSON 正则性能问题）
  2. `_find_company_list()` 递归遍历 JSON 树找第一个 `companyList` 列表
  3. `_normalize_company()` 标准化 30+ 字段
- DB 表：`tianyancha_search`（company_id/name/alias/legal_person_name/reg_capital/reg_status/credit_code/phone/分类/风险计数...）
- Registry auto-discovery 正确注册，URL 匹配正常
- 参考 HTML 解析验证通过：2 家公司数据正确提取

## 12:46 test-url 重构：复用 _browser_page_lifecycle / _fetch_http
- 问题：`fetch_raw_html` 手写了一遍 `_browser_page_lifecycle` 的钩子逻辑，两套代码维护成本高
- 重构：
  1. `_browser_page_lifecycle` 新增 `extra_timeout_ms: int = 0` 参数（show_window 时传 60000）
  2. `fetch_raw_html` browser 路径 → 直接调用 `_browser_page_lifecycle(browser, url, parser, proxy_url=None, cookies=None)`
  3. `fetch_raw_html` HTTP 路径 → 调 `_fetch_http({"id": 0, "url": url}, parser, proxy_record=None)`，catch `HTTPStatusError` 仍返回原始内容
  4. 删除独立的 `_fetch_raw_html_http` 方法（~40 行）
- 效果：test-url 现在享有与生产完全一致的 Cookie 注入/hooks/header 合并/params 支持（之前缺失）
- 全量 442 测试 PASS

## 12:50 test-url 移入 RequestPool.debug_parse()
- Flask handler 之前散落 120 行业务逻辑（parser 匹配/fetch_mode/FakeStorage/fetch/parse/降级路径）
- 新增 `RequestPool.debug_parse(url, registry, parser_name, show_window)` → 封装全部 5 步：匹配 Parser → 确定 fetch_mode → 注入 FakeStorage → fetch_raw_html → parse
- Flask handler 精简为 55 行纯适配层（取参数 → 调 debug_parse → 返 JSON）
- 删除 config_api.py 的 `_save_test_raw_response_static` 及 3 个降级路径（fallback browser/HTTP），raw 保存由 request_pool 统一处理
- 删除 config_api.py 不再需要的 imports：hashlib/datetime/urlparse/os/config

## 12:54 Code Review 校验修复（3 个问题）
1. **HTTP 状态码错误**：parser 匹配失败的 error 消息含 "Parser" 字样，被 `.lower() "parse" in` 误判为 parse 执行失败 → 全部返 500（应为 400/404）
   - 修复：只对 `error.startswith("Parse 失败")` 返回 500
2. **空 HTML 未校验**：`debug_parse` 未检查 fetch 结果为空 → `parser.parse("")` 产生混淆错误
   - 修复：fetch 后增加 `if not html: return {"ok": False, "error": "获取到空 HTML"}`
3. **preferred_fetch_mode 未生效**：`debug_parse` 只检查 `requires_browser`，忽略 `parser.preferred_fetch_mode`
   - 修复：`fetch_mode = parser.preferred_fetch_mode or config.get("fetch_mode", "browser")`（与 `_process_url_async` 一致）
- 全量 442 测试 PASS

## 12:41 test-url API 原始响应保存修复
- 问题：`/api/config/test-url` 不保存 raw HTML 到 `data/raw_responses/`
- 根因：`fetch_raw_html()` 只 return dict 不调用 `_save_raw_response`，与 FakeStorage 无关（FakeStorage 只影响 parser，不影响 request_pool）
- 修复 3 层：
  1. `core/request_pool.py` — 新增 `_save_test_raw_response()` 静态方法（URL host+md5+时间戳命名），`fetch_raw_html` 和 `_fetch_raw_html_http` 返回值加入 `raw_path`
  2. `web/api/config_api.py` — 新增 `_save_test_raw_response_static()` 辅助函数（fallback 路径用），API 响应加入 `raw_path` 字段（含 parse 错误分支）
  3. 新增 imports: `hashlib/datetime/urlparse` → config_api.py, 从 config 导入 `RAW_RESPONSE_DIR/PROJECT_ROOT`
