# 2026-06-22 工作日志

## 15:21 DataBrowserView 下拉框默认选第一项
- 文件：`web-ui/src/views/DataBrowserView.vue`
- 修改：`onMounted` 中 `fetchTables()` 后，若 `selectedTable` 为空（URL query 未指定），自动选 `tables[0].name`
- URL query 优先级保持不变（`restoreFromQuery()` 先执行）

## 15:26 删除 Parsers 管理页面死代码"切换"按钮
- 后端 `web/api/parsers.py`：删除空的 `toggle` 路由（`/<name>/toggle`，只返回 `{ok: true}` 无实际操作）
- 前端 `web-ui/src/views/ParsersView.vue`：删除 `doToggle` 函数和"切换"按钮
- 前端 API `web-ui/src/api/index.ts`：删除 `parsersApi.toggle()` 方法
- 重新构建前端 dist 并同步到 `web/static/`

## 16:23 反检测系统设计方案完成
- 产出：`docs/plans/2026-06-22-anti-detection-browser-cdp.md`
- 三模式架构：http / browser (Playwright+stealth) / cdp (连接本地 Chrome)
- 集成 fake-useragent 随机 UA，8 项新配置，CrawlerBrowserCDP 新类
- 9 个任务，涉及 6 个文件改动 + 2 个新测试文件
- 设计确认：Parser 级 preferred_fetch_mode + 三层优先级覆盖

## 16:44 反检测系统实现完成（Subagent-Driven）
- 9 个任务全部完成，425 测试全过（含 15 个新增）
- 三模式架构落地：http / browser(强化) / cdp(新增)
- Git commits: 9 个，从 ee2b2d2 到 857509e
- 关键变更:
  - fake-useragent 集成（HTTP + Browser 双通道随机 UA）
  - CrawlerBrowser 强化（_get_random_ua / random_delay / stealth 可配置）
  - CrawlerBrowserCDP 新类（connect_over_cdp 连接本地 Chrome）
  - RequestPool 三模式分支 + Parser 优先级
  - 8 项新配置（anti_bot_* + cdp_*）
  - main.py --fetch-mode cdp 支持

## 17:10~17:25 test-url 重设计（Subagent-Driven，全 7 任务完成，441 测试全过）
- 旧 test-url 裸返回 HTML，不经过 Parser，前端让用户手动选 Browser/HTTP 模式（设计错误）
- 新设计走完整 Parser pipeline：match parser → fetch_mode(requires_browser优先) → fetch → parse → 返回结构化 data
- 7 commits: 53c5b59 → acab754
- 关键变更:
  - `core/fake_storage.py` — FakeStorage 代理，屏蔽 enqueue/enqueue_image/save_business_data
  - `parser/plugins/shengyizr/_base.py` — SimplePageParser 标记 `requires_browser=True`
  - `core/request_pool.py` — 新增 `fetch_raw_html()` 方法，纯 HTML 获取无 DB 写入
  - `dev.py` — CRAWLER_COMPONENTS 注入 `request_pool`
  - `web/api/config_api.py` — 重写 test-url：接收可选 `parser` 参数，返回 parse 结果
  - 前端：移除 mode 切换 + HTTP 配置区，传 parser 参数，展示解析结果表格
  - 测试：新增 16 个 FakeStorage 测试，全量 441 过

## 18:48 storage upsert + request_pool 空数据校验
- `core/storage.py:518`: `INSERT INTO` → `INSERT OR REPLACE INTO`（commit 0afda88）
  - 唯一键冲突时自动替换旧行，重试任务不再报 UNIQUE constraint failed
  - 新增测试 `test_save_business_data_upsert_on_duplicate_unique`
- `core/request_pool.py:_finish_request`（commit ad99052）
  - `data` 为 `[]` 或 `None` 时 → `mark_failed` + `ERROR_PARSE` + `_release_proxy(success=False)`
  - 之前无论数据多空都算 success，现在空结果直接标记失败
  - `len(data)` 加了 None 保护
  - 新增 `tests/test_request_pool.py`，3 个测试全过
- 全量 445 测试通过

## 18:42 save_business_data 改为 upsert 语义
- 文件：`core/storage.py:518`
- 问题：重试任务时 `save_business_data()` 纯 `INSERT INTO` 遇 UNIQUE 约束报错
- 修复：改为 `INSERT OR REPLACE INTO`，唯一键冲突时自动替换旧行
- 测试：新增 `test_save_business_data_upsert_on_duplicate_unique`，全部 6 个 save_business_data 测试通过

## 19:49 on_wait_ready 钩子（commit 2050e8c）
- `parser/base.py`：新增 `on_wait_ready(page)` 空实现，子类可覆盖
- `core/request_pool.py`：CDP/Browser/fetch_raw_html 三处，在 on_page_loaded + captcha 后、HTML 提取前调用
- `shengyizr/list.py`：等待 `div.content-side-left li h2`
- `shengyizr/detail.py`：等待 `div.house-title h1`
- 解决 redirect guard 还在跳转时就提前 parse 的问题
- 445 测试全过

## 20:18 修复 ProxyPool.acquire() 同步补充阻塞事件循环
- **根因**：`pool.py:112` acquire() 内同步调 `_supplement()` → `provider.fetch()` → httpx.Client（同步，默认 10s timeout），阻塞整个 asyncio 事件循环
- **修复**：
  - `acquire()` 移除同步 `_supplement()` 调用，纯查询不阻塞
  - 新增 `warmup()` 方法 — 冷启动同步预填充（在事件循环开始前调用）
  - 新增 `start_supplement_loop()` / `stop_supplement_loop()` — 后台线程每 `proxy_supplement_interval`（默认 5s）检查低水位并补充
  - `_supplement()` 加 `_supplement_lock`（非阻塞 acquire）防止并发补充
  - 修正命名：`_supplement_thread` → `_health_thread`，`_supplement_stop` → `_stop_event`（共享）
- **接入**：`main.py` 启动时 warmup() + start_supplement_loop()，finally 里 stop_supplement_loop()
- **配置**：新增 `proxy_supplement_interval`（默认 5s）
- **测试**：36 个 proxy_pool 测试全过，全量 449 passed

## 20:30 Code Review fix（干净上下文子代理审查 + 修复）
- **审查方式**：子代理纯净上下文审查，发现 4 个 P1 + 3 个 P2
- **P1-1** `_supplement` 缺 enabled 检查 → 已加 guard
- **P1-2** `stop_*_loop` join 超时丢引用 → 10s 超时 + is_alive 恢复引用
- **P1-3/4** 两个 flaky 测试 → 设 supplement_interval=1 加速 + 修复逻辑
- **P0** shared `_stop_event` 拆为 `_health_stop` + `_supplement_stop`
- **P2** 注释 "25 项" → 改为通用；新增 warmup 禁用测试

## 20:39 巨量HTTP Provider 改写
- **问题**：`JuliangProvider` 响应解析与真实 API 完全不匹配（JSON 期望 `{data:[{ip,port}]}` 实际是 `{data:{proxy_list:[["ip",port,"user","pass"]]}}`，text 格式期望 `ip:port` 实际是 `ip:port:user:pass`，ProxyRecord 缺 username/password 字段）
- **修复**：
  - `ProxyRecord` 加 `username`/`password` 可选字段
  - `_do_fetch` 参数 `format`→`result_type`，删不存在的 `tt` 参数
  - `_parse_json` 重写为 `data.proxy_list` 数组格式，加 code 非 200 校验
  - `_parse_text` 支持 2~4 段格式
  - `pool.py` INSERT/SELECT 加 username/password 列
  - `request_pool.py` 新增 `_build_proxy_url` 拼接账密（`http://user:pass@ip:port`）
  - `storage.py` proxy_pool 表加 username/password 列 + 迁移
  - 测试：原 3 个误测 → 7 个正确测试（text basic + text auth + json + json noauth + bad code + invalid items + kuaidaili），全量 453 通过

## 20:42~20:58 IP池动态缩减 + 闲置保护
- `_supplement(num=needed)`：按差额补而不是盲拉 fetch_num
- `fetch_num` 默认 10→3
- 补充线程加闲置检测：`_last_acquire_time > ttl` 时跳过（爬虫停了不白花钱）
- 前端 ConfigView 加 `proxy_supplement_interval`，ProxyPoolView 加"账密"列

## 21:27~21:41 IP池 JIT 重构 (Subagent-Driven, 6 tasks)
- **设计重定位**：按用户意图，IP池从"预填+后台补"改为"用时才买"
- **Task 1** Provider 加 `fetch_async`/`_do_fetch_async`（JuliangProvider 用 httpx.AsyncClient）
- **Task 2+3** `acquire()` → `acquire_async()`；新增 `_check_ip_alive()` 连通性检测；删 warmup/supplement_loop/_supplement
  - acquire_async 流程：查池 → 有则返回 → 池空 await fetch_async(1) → 连通性检测 → 入库 → 返回；最多 3 次重试
- **Task 4** `request_pool.py`: `acquire()` → `await acquire_async()`
- **Task 5** 清理：删 proxy_supplement_interval 配置/UI/main.py 调用
- **最终测试**：451 passed, 1 skipped
- 代码评审发现的 3 个问题已修复（I3 last_used_at、I4 INSERT try/except、M1 WARNING→debug）

## 22:16 子代理代码审查 + 修复
- **C2 (Critical)** `acquire_async` 加 `asyncio.Lock` + 双重检查，防并发买 IP
- **C3 (Critical)** `KuaidailiProvider` 补 `_do_fetch_async`，防异步上下文阻塞
- **I1 (Important)** INSERT 改 `INSERT OR IGNORE` + rowcount 检查
- **I2 (Important)** `_check_ip_alive` 检测 URL 改为可配置 `proxy_test_url`
- **I4 (Important)** `stop_health_check_loop` 不再恢复 hung 线程引用
- **M1/M5/M7** 清理未用 import + 新增 disabled/concurrent 测试
- **最终**：453 passed, 1 skipped
